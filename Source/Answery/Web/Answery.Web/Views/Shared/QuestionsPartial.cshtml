@model IEnumerable<Answery.Web.ViewModels.Question.QuestionViewModel>
<div>
    @if (Model.Count() == 0)
    {
        @Html.Partial("NoQuestionsPartial")
    }
    else
    {
        foreach (var question in Model)
        {
            <div id="profile-page-wall-posts" class="row">
                <div class="col s12">
                    <div id="profile-page-wall-post" class="card">
                        <div class="card-profile-title">
                            <div class="row">
                                <div class="col s10">
                                    <span class="grey-text text-darken-1 ultra-small">Asked on @question.CreatedOn.ToString("dd MMMM yyyy hh:mm")</span>
                                </div>
                            </div>
                            <div class="row">
                                <div class="col s12 black-text">
                                    <p class="flow-text">
                                        @question.Content
                                    </p>
                                </div>
                            </div>
                            <hr class="black"/>
                            <div class="row">
                                <div class="col s10">
                                    <span class="text-darken-2 ultra-small light-green-text">Answered on @if (question.AnsweredOn != null)
                                                                                                  {
                                                                                                      <span>@question.AnsweredOn.Value.ToString("dd MMMM yyyy hh:mm")</span>
                                                                                                  }
                                    </span>
                                </div>
                            </div>
                            <div class="row">
                                <div class="col s12 black-text">
                                    <p class="bold">
                                        @question.Answer
                                    </p>
                                </div>
                            </div>
                            <div class="row">
                                <div class="col s2 offset-s10">
                                    <span class="grey-text text-darken-1 ultra-small">
                                    @if (question.Comments != null)
                                    {
                                        foreach (var comment in question.Comments)
                                        {
                                            <div>comment.Content</div>
                                        }
                                    }



                                     @*@if (question.Comments != null)
                                     {
                                         <div>@question.Comments.Count() comments</div>
                                     }*@</span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        }
    }
</div>